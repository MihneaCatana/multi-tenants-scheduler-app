# Frontend dev config. Copy to `.env` to override.
# Base domain MUST match the backend's BASE_DOMAIN (subdomain-based tenancy).
VITE_BASE_DOMAIN=simisolutions.localhost
# Dev server port (the URL you open in the browser, e.g. *.simisolutions.localhost:5173).
VITE_PORT=5173
